apiVersion: v1
kind: Service
metadata:
  name: inventory-microservice
  namespace: warehouse-management-namespace
spec:
  selector:
    app: inventory-microservice
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
