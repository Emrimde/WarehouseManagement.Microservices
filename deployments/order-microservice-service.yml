apiVersion: v1
kind: Service
metadata:
  name: order-microservice
  namespace: warehouse-management-namespace
spec:
  selector:
    app: order-microservice
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
