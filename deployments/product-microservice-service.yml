apiVersion: v1
kind: Service
metadata:
  name: product-microservice
  namespace: warehouse-management-namespace
spec:
  selector:
    app: product-microservice
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
