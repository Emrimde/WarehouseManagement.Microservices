apiVersion: v1
kind: Service
metadata:
  name: order-postgres
  namespace: warehouse-management-namespace
spec:
  selector:
    app: order-postgres
  ports:
  - protocol:  TCP
    port: 5432
    targetPort: 5432
  type: ClusterIP