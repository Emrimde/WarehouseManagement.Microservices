apiVersion: v1
kind: Service
metadata:
  name: product-postgres
  namespace: warehouse-management-namespace
spec:
  selector:
    app: product-postgres
  ports:
  - protocol:  TCP
    port: 5432
    targetPort: 5432
  type: ClusterIP
